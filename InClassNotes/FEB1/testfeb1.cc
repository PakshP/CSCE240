#include <iostream>

int main() {
    int x = 5;
    if ( x = 0)
        std::cout << "x is zero" << std::endl;
    else
        std::cout << "x is not zero" << std::endl;
    std::cout << "x is " << x << std::endl;
    return 0;
}